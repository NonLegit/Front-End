import {
  Box, styled,
} from '@mui/material';

export const StyledBox = styled(Box)(() => ({
  padding: '16px',
  display: 'flex',
  width: '100%',
  justifyContent: 'space-between',
  flexDirection: 'column',
}));
