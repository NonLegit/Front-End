import {
  Box,
} from '@mui/material';
import { styled } from '@mui/material/styles';

export const CheckBoxConatiner = styled(Box)(() => ({
  width: '100%',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'flex-start',
}));
